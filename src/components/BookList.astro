---
import GeneralLayout from "../layouts/GeneralLayout.astro";

const { bookTitle, dateRead, bookImage, bookAuthor, status, audiobook, didntFinish, bookYear } = Astro.props;
const pageTitle = "Bookshelf";
---

<GeneralLayout pageTitle={pageTitle}>
  <slot />
</GeneralLayout>

<!-- <style>
  .bookImage {
    box-shadow: 2px 2px 3px rgba(0, 0, 0, 0.3);
  }
</style> -->
